# Optional. Only needed for provider=openai_compatible.
RLM_API_KEY=replace-with-your-provider-key

# Optional examples:
# RLM_PROVIDER=openai_compatible
# RLM_ENDPOINT=https://api.example.com/v1/chat/completions
# RLM_MODEL=provider-model-name

